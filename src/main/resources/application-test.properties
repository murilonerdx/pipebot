# Usa banco de memória / mockado (desabilita mongo real)
spring.data.mongodb.auto-index-creation=true
spring.data.mongodb.database=pipebot-test

# NÃO definir host/port ? evita tentativa de conexão
spring.data.mongodb.uri=mongodb://localhost:0/test
